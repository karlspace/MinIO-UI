# MinIO Console Environment Configuration
# Copy this file to .env and adjust the values

# Required: MinIO Server Endpoint
# Examples:
#   - http://localhost:9000
#   - http://minio:9000 (when using docker-compose)
#   - https://minio.example.com
CONSOLE_MINIO_SERVER=http://minio:9000

# Optional: JWT encryption secrets
# If not set, random values will be auto-generated on startup
# WARNING: Sessions will be lost on container restart without these!
# Generate secure values with: openssl rand -base64 32
# CONSOLE_PBKDF_PASSPHRASE=
# CONSOLE_PBKDF_SALT=

# Optional: Console HTTP Port (default: 9090)
# CONSOLE_PORT=9090

# Optional: Console HTTPS Port (default: 9443)
# CONSOLE_TLS_PORT=9443

# Optional: Debug logging level (0-6, default: 0)
# 0 = no logging
# 1 = log server errors (5xx)
# 2 = log client and server errors (4xx/5xx)
# 3 = log all requests
# 4 = detailed server errors
# 5 = detailed client and server errors
# 6 = detailed all requests
# CONSOLE_DEBUG_LOGLEVEL=0

# Optional: TLS Certificate paths (if using custom certificates)
# CONSOLE_TLS_CERTIFICATE=/home/console/.console/certs/public.crt
# CONSOLE_TLS_KEY=/home/console/.console/certs/private.key
